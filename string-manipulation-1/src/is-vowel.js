/* exported isVowel */
// function isVowel(char) {
//   var vowels = [ ];
//   if (char === vowels) {
//     return true;
//   } else {
//     return false;
//   }
// }
function isVowel(char) {
  return char === 'a' || char === 'A' || char === 'e' || char === 'E' || char === 'i' || char === 'I' || char === 'o' || char === 'O' || char === 'u' || char === 'U' || false;
}
